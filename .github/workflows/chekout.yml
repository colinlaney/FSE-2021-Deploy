on:
  push
jobs:
  idle:
    name: Checkouts
    runs-on: ubuntu-latest
    steps:
      -
        name: Pull the repository
        uses: actions/checkout@v2
      -
        run: ls
      -
        uses: actions/checkout@v2
        with:
          repository: niessner/BundleFusion
          path: other_repo
      -
        run: ls other_repo
